<style>
	.outer {
		margin: auto;
		text-align: center;
	}

	.inner {
		display: inline-block;
		font-size: 1.25em;
	}

	.form {
		display: block;
		text-align: center;
	}

	.form > div > input {
		display: block;
		margin: 0.5em 0;
		padding: 0.25em 0.75em;
		font-size: 1em;
		width: 12em;
		max-width: 100%;
		border-radius: 1em;
	}

	.form > div {
		display: inline-block;
	}
	
	.form > div > button {
		padding: 0.25em 0.75em;
		font-size: 1em;
		border-radius: 1em;
		cursor: pointer;
	}

	.sign_up {
		display: block;
		margin-top: 0.5em;
		font-size: 0.75em;
	}
</style>

<div class="outer">
	<div class="inner">
		<%= form_for(@conn, Routes.user_path(@conn, :sign_in), [class: "form"], fn f -> %>
			<div>
				<%= text_input(f, :username, placeholder: "username") %>
				<%= text_input(f, :password, placeholder: "password", type: "password") %>
				<%= submit "sign in" %>
			</div>
		<% end) %>

		<span class="sign_up">don't have an account? sign up <%= link("here", to: "/sign_up") %></span>
	</div>
</div>

<link rel="stylesheet" type="text/css" href="/css/sign_in.css">
